---
interface Props {
  date: Date;
}

const { date, ...rest } = Astro.props;

const datetime = date.toISOString();
---

<time
  datetime={datetime}
  {...rest}
>
  {datetime.substring(0, 10)}
</time>
