---
import { isPastYear } from "../lib/is-past-year";
interface Props {
  publishDate: Date;
}

const { publishDate } = Astro.props;
const now = new Date();
---

{
  isPastYear(publishDate, now) && (
    <span class="text-red-600">(Updated more than a year ago)</span>
  )
}
